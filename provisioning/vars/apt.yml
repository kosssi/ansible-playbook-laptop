---

apt_repositories:
  - ppa:ansible/ansible
  - ppa:ondrej/php5

apt_install:
  - python-apt
  - unattended-upgrades
  - openjdk-7-jre
  - tmux
  - vim
  - nmap
  - unrar

  # devlopment
  - ansible
  - nfs-server

  # Web
  - chromium-browser

  # Multimedia
  - vlc

  # php devlopment
  - php5
  - php5-intl
  - php5-curl

apt_remove:
  # replace by nvm
  - nodejs
